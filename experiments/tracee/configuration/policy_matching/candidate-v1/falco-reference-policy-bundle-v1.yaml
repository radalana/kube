# Generated by Chagbt 5.6 sol
# Tracee 0.24.1 candidate Falco-reference policy bundle
#
# Reference: enabled rules in falco_rules(1).yaml
# Tracee CRD: tracee.aquasec.com/v1beta1, kind Policy
#
# IMPORTANT:
# - This is a runtime-event mapping, not a claim of semantic identity.
# - "Partial" mappings intentionally collect the closest Tracee event and require
#   remaining Falco conditions to be checked during analysis.
# - Tracee 0.24.1 built-in security events are used where they provide the closest
#   existing detector.
# - Validate the bundle in your installed 0.24.1 environment before final runs.

apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-directory-traversal-read
  annotations:
    description: "Falco reference: Directory traversal monitored file read; raw open-path candidate events."
spec:
  scope:
    - global
  rules:
    - event: open
    - event: openat
    - event: openat2
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-sensitive-read-trusted
  annotations:
    description: "Falco reference: Read sensitive file trusted after startup; sensitive file-open candidate events."
spec:
  scope:
    - global
  rules:
    - event: security_file_open
      filters:
        - data.pathname=/etc/shadow,/etc/sudoers,/etc/pam.conf,/etc/security/pwquality.conf,/etc/sudoers.d/*,/etc/pam.d/*
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-sensitive-read-untrusted
  annotations:
    description: "Falco reference: Read sensitive file untrusted; trusted-process exclusions require analysis-time correlation."
spec:
  scope:
    - global
  rules:
    - event: security_file_open
      filters:
        - data.pathname=/etc/shadow,/etc/sudoers,/etc/pam.conf,/etc/security/pwquality.conf,/etc/sudoers.d/*,/etc/pam.d/*
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-run-shell-untrusted
  annotations:
    description: "Falco reference: Run shell untrusted; mapped to Tracee built-in illegitimate_shell."
spec:
  scope:
    - global
  rules:
    - event: illegitimate_shell
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-system-user-interactive
  annotations:
    description: "Falco reference: System user interactive; ancestry/user criteria require analysis-time correlation."
spec:
  scope:
    - global
  rules:
    - event: sched_process_exec
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-terminal-shell-container
  annotations:
    description: "Falco reference: Terminal shell in container; TTY and container-entrypoint conditions are not available as equivalent policy filters."
spec:
  scope:
    - global
  rules:
    - event: sched_process_exec
      filters:
        - container
        - comm=ash,bash,csh,ksh,sh,tcsh,zsh,dash
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-k8s-api-contact
  annotations:
    description: "Falco reference: Contact K8S API Server From Container; mapped to Tracee built-in k8s_api_connection."
spec:
  scope:
    - global
  rules:
    - event: k8s_api_connection
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-netcat-rce
  annotations:
    description: "Falco reference: Netcat Remote Code Execution in Container; execution-option check requires analysis-time correlation."
spec:
  scope:
    - global
  rules:
    - event: sched_process_exec
      filters:
        - container
        - comm=nc,ncat
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-private-key-search
  annotations:
    description: "Falco reference: Search Private Keys or Passwords; key/password argument patterns require analysis-time correlation."
spec:
  scope:
    - global
  rules:
    - event: sched_process_exec
      filters:
        - comm=grep,egrep,fgrep,find
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-clear-log
  annotations:
    description: "Falco reference: Clear Log Activities; verify write/truncate flags and exclusions during analysis."
spec:
  scope:
    - global
  rules:
    - event: security_file_open
      filters:
        - data.pathname=/var/log/*,/dev/log/*
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-remove-bulk-data
  annotations:
    description: "Falco reference: Remove Bulk Data from Disk."
spec:
  scope:
    - global
  rules:
    - event: sched_process_exec
      filters:
        - comm=shred,mkfs,mke2fs
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-symlink-sensitive
  annotations:
    description: "Falco reference: Create Symlink Over Sensitive Files; verify target path during analysis."
spec:
  scope:
    - global
  rules:
    - event: symlink
    - event: symlinkat
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-hardlink-sensitive
  annotations:
    description: "Falco reference: Create Hardlink Over Sensitive Files; verify oldpath target during analysis."
spec:
  scope:
    - global
  rules:
    - event: link
    - event: linkat
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-packet-socket
  annotations:
    description: "Falco reference: Packet socket created in container; verify AF_PACKET from socket domain argument."
spec:
  scope:
    - global
  rules:
    - event: socket
      filters:
        - container
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-stdio-network-redirect
  annotations:
    description: "Falco reference: Redirect STDOUT/STDIN to Network Connection in Container; mapped to Tracee built-in stdio_over_socket."
spec:
  scope:
    - global
  rules:
    - event: stdio_over_socket
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-kernel-module
  annotations:
    description: "Falco reference: Linux Kernel Module Injection Detected; mapped to Tracee built-in kernel_module_loading and limited to container context."
spec:
  scope:
    - global
  rules:
    - event: kernel_module_loading
      filters:
        - container
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-debugfs-privileged
  annotations:
    description: "Falco reference: Debugfs Launched in Privileged Container; privileged-state check remains analysis-time."
spec:
  scope:
    - global
  rules:
    - event: sched_process_exec
      filters:
        - container
        - comm=debugfs
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-release-agent
  annotations:
    description: "Falco reference: Detect release_agent File Container Escapes; mapped to Tracee built-in cgroup_release_agent."
spec:
  scope:
    - global
  rules:
    - event: cgroup_release_agent
      filters:
        - container
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-ptrace-attach
  annotations:
    description: "Falco reference: PTRACE attached to process; request values 4,5,13,16,16902 correspond to Falco's selected attach/injection request set."
spec:
  scope:
    - global
  rules:
    - event: ptrace
      filters:
        - data.request=4,5,13,16,16902
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-ptrace-antidebug
  annotations:
    description: "Falco reference: PTRACE anti-debug attempt; mapped to Tracee built-in anti_debugging."
spec:
  scope:
    - global
  rules:
    - event: anti_debugging
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-aws-credential-search
  annotations:
    description: "Falco reference: Find AWS Credentials; AWS-specific argument patterns require analysis-time correlation."
spec:
  scope:
    - global
  rules:
    - event: sched_process_exec
      filters:
        - comm=grep,egrep,fgrep,find
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-exec-dev-shm
  annotations:
    description: "Falco reference: Execution from /dev/shm; uses resolved pre-exec pathname."
spec:
  scope:
    - global
  rules:
    - event: security_bprm_check
      filters:
        - data.pathname=/dev/shm/*
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-drop-execute-container
  annotations:
    description: "Falco reference: Drop and execute new binary in container; upper-layer condition requires analysis-time correlation. Tracee dropped_executable is related but not semantically identical."
spec:
  scope:
    - global
  rules:
    - event: sched_process_exec
      filters:
        - container
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-ssh-nonstandard-port
  annotations:
    description: "Falco reference: Disallowed SSH Connection Non Standard Port; verify outbound TCP destination/port during analysis."
spec:
  scope:
    - global
  rules:
    - event: connect
      filters:
        - comm=ssh
---
apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: falco-ref-memfd-execution
  annotations:
    description: "Falco reference: Fileless execution via memfd_create; mapped to Tracee built-in fileless_execution."
spec:
  scope:
    - global
  rules:
    - event: fileless_execution
