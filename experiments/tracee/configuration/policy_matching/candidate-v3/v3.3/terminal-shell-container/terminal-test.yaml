apiVersion: v1
kind: Pod
metadata:
  name: terminal-test
  namespace: default
spec:
  nodeName: worker1
  restartPolicy: Never
  containers:
    - name: test
      image: alpine:3.22
      command:
        - /bin/sh
        - -c
        - sleep 600