apiVersion: v1
kind: Pod
metadata:
  name: tracee-aws-negative
  namespace: default
spec:
  nodeName: worker1
  restartPolicy: Never
  containers:
    - name: test
      image: alpine:3.22
      command:
        - /bin/sh
        - -c
        - |
          echo "ordinary_setting = TEST" > /tmp/config
          grep "ordinary_setting" /tmp/config
          sleep 3