apiVersion: v1
kind: Pod
metadata:
  name: terminal-validation
  namespace: tetragon-policy-validation
spec:
  containers:
  - name: alpine
    image: alpine
    command: ["sleep", "3600"]
  restartPolicy: Never